with open("new.txt")as stream:
         data=stream.readlines()

print data

for value in data:
        print value.strip("\n")
           try:
                   with zipfile("challenge1.zip")as zf:
                   zfextractall(pwd="")== None()
           except RuntimeError:
                    print"extraction failed"           
